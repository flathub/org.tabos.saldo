# Use the following command to regenerate Python modules
# flatpak-pip-generator --runtime=org.gnome.Sdk//50 -r requirements.txt --prefer-wheels=cryptography

cryptography
fints
enum-tools
mt-940
schwifty
